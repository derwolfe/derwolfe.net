Blog
====

Mynty fresh choices.

Rebuilding the repository
-------------------------
1. Make a virtualenv
2. `pip install -r requirements.txt`
3. `gem install bundler`
4. `bundle install`
5. `make build` to build the assets
6. `make serve` to start up twisted and serve the contents of `_built`
